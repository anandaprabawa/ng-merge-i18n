/**
 * Builder options of the target `extract-i18n` on `angular.json` file.
 */
export interface ExtractI18nBuilderOptions {
  browserTarget: string;
  format?: string;
  outputPath?: string;
  outFile?: string;
}
