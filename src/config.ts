/**
 * Default format of extracted Angular translation when not providing
 * `format` option in `extract-i18n` builder target.
 */
export const DEFAULT_EXTRACTED_FORMAT = "xlf";

/**
 * Default source locale of the angular i18n.
 */
export const DEFAULT_SOURCE_LOCALE = "en-US";

/**
 * Supported formats of translation that can be merged.
 */
export const SUPPORTED_FORMATS = ["json"];
